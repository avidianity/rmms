import React, { FC } from 'react';

type Props = {};

const Prescriptions: FC<Props> = (props) => {
	return <div className='container-fluid'>Under Development</div>;
};

export default Prescriptions;
